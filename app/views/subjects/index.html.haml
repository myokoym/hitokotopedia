%h1 一言で〇〇を説明してください。

%table
  %tr
  - @subjects.each do |s|
    %tr
      %td= link_to s.name + " とは？", :controller => :subjects, :action => :show, :id => s.id

%h2 新規登録
- form_for @description do |f|
  %p
    = f.label "対象"
    %br
    = f.text_field :subject
    %br
  %p
    = f.label "説明"
    %br
    = f.text_area :description, :rows => 3
    %br
  = f.submit "送信"

